name: "Legacy PHP Docker Images delivery"

on:
  schedule:
    # once per month
    - cron: "15 4 1 * *"
  push:
    tags:
      - "v*.*.*"

jobs:
  php80-cli-image-build:
    runs-on: ubuntu-latest
    steps:
    - 
      name: Checkout code
      uses: actions/checkout@v4
    - 
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    - 
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - 
      name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Preparing image meta
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: milejko/php
        tags: |
          type=raw,value=8.0-cli-{{date 'YYYYMMDD'}}
          type=raw,value=8.0-cli
    - 
      name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        target: php-cli
        tags: ${{ steps.meta.outputs.tags }},
        labels: ${{ steps.meta.outputs.labels }},
        build-args: PHP_VERSION=8.0

  php80-fpm-image-build:
    runs-on: ubuntu-latest
    steps:
    - 
      name: Checkout code
      uses: actions/checkout@v4
    - 
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    - 
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - 
      name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Calculating image meta - tags & labels
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: milejko/php
        tags: |
          type=raw,value=8.0-fpm-{{date 'YYYYMMDD'}}
          type=raw,value=8.0-fpm
    - 
      name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        target: php-fpm
        tags: ${{ steps.meta.outputs.tags }},
        labels: ${{ steps.meta.outputs.labels }},
        build-args: PHP_VERSION=8.0
  php74-cli-image-build:
    runs-on: ubuntu-latest
    steps:
    - 
      name: Checkout code
      uses: actions/checkout@v4
    - 
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    - 
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - 
      name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Calculating image meta - tags & labels
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: milejko/php
        tags: |
          type=raw,value=7.4-cli-{{date 'YYYYMMDD'}}
          type=raw,value=7.4-cli
    - 
      name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        target: php-cli
        tags: ${{ steps.meta.outputs.tags }},
        labels: ${{ steps.meta.outputs.labels }},
        build-args: PHP_VERSION=7.4

  php74-fpm-image-build:
    runs-on: ubuntu-latest
    steps:
    - 
      name: Checkout code
      uses: actions/checkout@v4
    - 
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    - 
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - 
      name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Preparing image meta
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: milejko/php
        tags: |
          type=raw,value=7.4-fpm-{{date 'YYYYMMDD'}}
          type=raw,value=7.4-fpm
    - 
      name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        target: php-fpm
        tags: ${{ steps.meta.outputs.tags }},
        labels: ${{ steps.meta.outputs.labels }},
        build-args: PHP_VERSION=7.4

